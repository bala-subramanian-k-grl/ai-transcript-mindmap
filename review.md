add logs as per the instruction
read two paragraphs from the article as per the instructions shared to give input to this app
save input audio in mp3 format
export output in mermaid code
add a button in the UI to export mindmap in mermaid and json and render in UI
